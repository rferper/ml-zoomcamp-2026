[project]
name = "09-serverless"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rferper", email = "raquelfernandezperalta@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
09-serverless = "09_serverless:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
