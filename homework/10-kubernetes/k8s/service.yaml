apiVersion: v1
kind: Service
metadata:
  name: clothing-classifier
  labels:
    app: clothing-classifier
spec:
  type: NodePort
  selector:
    app: clothing-classifier
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080